language: c

matrix:
  include:
    - os: linux
      dist: xenial
    - os: osx
      osx_image: xcode9.4

addons:
  apt:
    packages:
     - cmake
     - yasm

jobs:
  include:
   - name: SVT-AV1
     script:
     - cd Build/linux
     - ./build.sh release
